# Repository harness upgrade

This domain governs independently authorized standard-root evaluator transitions without allowing candidate source or candidate packages to govern their own adoption.

## Completed 0.5.0 transition

- `INT-HUP-001` through `VER-HUP-001` are approved, and `WO-HUP-001` is `implemented` with retained local evidence for the upgrade from released bootstrap `0.5.0a1` to immutable public `0.5.0`.
- Candidate commit, hosted acceptance, VREC preparation/transition, push, pull request, and merge remained separate decisions.

## Implemented 0.6.0 transition

- `INT-HUP-002`, `CAP-HUP-002`, `REQ-HUP-004` through `REQ-HUP-006`, `SPEC-HUP-002`, `ARCH-HUP-002`, `VER-HUP-002`, and `WO-HUP-002` are approved. The exact transaction is applied and `WO-HUP-002` is `implemented`.
- `REQ-HUP-007`, `SPEC-HUP-003`, `VER-HUP-003`, and `WO-HUP-003` are approved. The exact eight-path compatibility plan reconciled the post-adoption source-suite assumptions without changing the immutable governor transaction or product runtime, and `WO-HUP-003` is `implemented`.
- The proposed operation uses the immutable public 0.6.0 wheel outside the checkout, binds the exact current lock and target payload/archive identity, applies the reviewed 18-change managed plan, advances the lock to schema 3, and retains keyed evaluator-upgrade evidence.
- Existing `docs/engineering/REPOSITORY_CONTEXT.md` bytes remain owner-controlled and unchanged while its withdrawn harness ownership is removed from the lock.
- The packet authorizes start preflight, the exact bound apply, local verification, and evidence retention. Candidate commit, VREC, push, pull request, merge, release, publication, deployment, and external actions remain separate decisions.
