This external replay invocation stopped during fixture validation, before any
replayed command. The frozen input bundle already contained the observer's
additional injected-partial-state observation, but its native Linux repository
had not yet been reconstructed. `summary.json` retains the original failure and
empty command list. This is a replay setup ordering error, not a skill or
evaluator failure. The missing recorded group was subsequently reconstructed;
the retry uses a fresh `external-attempt2` output directory.
