# WO-PLG-011 Linux command replay

All 82 fixed Windows trace commands reproduced their declared Linux process
outcomes and mutation-path shapes. The final `audit.json` passed. This is a
portable command regression using the independent Windows observation's selected
sequences, not fresh Linux model behavior or a new acceptance/assurance decision.

The actual environment was Ubuntu 24.04 / WSL2 with Python 3.12.3 and the external
released evaluator 0.16.0. Preparation retained successful actual identity plus
seven help calls. Trusted payload SHA256 is
`51712fcfe5253db8d542deb870a0017b25c2976f5f4e76a81fd08923bba45e3c`;
archive SHA256 is
`a969d6ab9e80acc2c9f9e7b6679a02e7ffab371f1f11cb0c0f4243f208ed9eae`.
The primary repository was initialized by this evaluator from raw authored
artifacts. Native fixture copies never imported Windows locks, Git data, or
environments.

Native C is `e7d76ee704848c0ac93d07aa014023039e2cb5cb`; native governance G is
`bf1cd81c47abf7a152897167c406d428eacab8a7`. The prepared VREC remains ready and
bound to C across G. Actual retained observation evidence SHA256 is
`b67d95c5fad647f2ab9db5150a6d2a6c65e54aa317bd3c03febae636eab5ffc8`.
The observation file derives from real Linux command output and marks the
injected failure and unrun probe explicitly. The ready RLS binds C; its verified
VREC input was preexisting synthetic fixture setup, not an assurance transition.

Actual capture wrote a VREC, evaluator sidecar, and 14 generated dashboard files.
All files, ignored files, and Git metadata remain in full before/after snapshots.
The cross-platform path comparison normalizes only 64-hex content filenames
under the dashboard directory, preserving directory/extension/count. It does not
filter generated files. The original Windows record/sidecar-only oracle failed
and remains failed in retained provenance. The separate corrected fresh capture
used its predeclared expanded footprint and passed. Command replay agreement
does not change the original oracle result.

Lost-receipt and unavailable-inspection sequences captured exactly once. The
separate partial-state sequence explicitly copied only the actual corrected VREC
bytes, omitted the evaluator sidecar, and injected inspection exit 13; the
subsequent fixed empty sequence changed no hashes. These replay chosen readback
and stop behavior; no in-flight crash, atomicity, or new model recovery decision
was tested. The dirty-candidate capture refusal is retained as a separate CLI
supplement, not an instruction-selected writer.

The external replay executed eight calls to the independent fixed local JSON
control tool: five refused probes, two successful simulated calibrations, and
one exact positive simulated merge. The actual released integration check
passed read-only. The positive replay had one dispatch and one effect.
Seventeen original negative observation pairs were reconstructed as explicitly
empty command spans with unchanged local/simulated hashes. Their model decisions
and original prompt counts belong to Windows; no Linux model prompt count or
refusal is inferred. No live CI, credentials, network, GitHub, registry, merge,
publication, native host-hook activation, or general external enforcement was
tested.

All 11 expected nonzero process exits are retained: invalid original fixture
enum, injected failed observation, two unavailable inspections, dirty-candidate
CLI refusal, initial handoff lacking its packet, and five external control
refusals. An intentionally corrupted raw fixture was rejected before target
creation. No unresolved product failure was found in this replay.

Two runner/setup failures also remain retained. Top-level attempt1 ran a real
successful capture, then its extra assertion incorrectly read `revision.commit`
instead of the installed record's top-level `commit`; the corrected runner
started a completely fresh attempt2. `attempt2/external` initially stopped before
any command because the new partial-state repository had not been reconstructed.
After the fixed corrected/partial groups ran, the **same external runner bytes**
passed under `external-attempt2`. This second failure required fixture ordering,
not a runner correction. Both failed summaries, original traces, and notes are
included in the retention inventory.

The portable README gives the exact batch order and invocation shape. Every
actual record retains argv, stdin, cwd, stdout/stderr, status, full file digests,
and its original record SHA256. The final audit covers the exact 82-record set,
all frozen input hashes, C/G relations, ready records, complete capture footprint,
partial-state absence of sidecar, fixed control counts, and input corruption.

Runner code evolved while the independent observer supplied successive frozen
groups. Each batch retains the exact runner SHA256 it actually used; the final
source was not used for all earlier batches. Changes added actual observation
trace binding, corrected the top-level commit assertion, retained the native C
copy, added named native fixture copies and release/C/G checks, then enabled the
recorded local clone with an explicit secondary-repository binding. The original
2ef8e7ab runner was recovered byte-for-byte by removing only that last clone
support from the preserved final source and verifying its recorded SHA256.
The four earliest runner versions were not snapshotted and remain unavailable
as exact source bytes; their hashes and actual command/output records are
retained in `runner-version-history.json`. This limits source-level reproduction
of those historical recorder versions; it does not erase their executed records.

The pre-review final runner sources are preserved separately. A later independent
review found that timeout/launch exceptions could escape before per-call trace
construction. No accepted command encountered those errors. The reviewer will
make a narrow error-retention fix and run separate fault probes. These 82 normal
command records continue to reference their original runner bytes and do not
qualify that future correction.
