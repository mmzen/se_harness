# Live delegated delivery

The released 0.17.0 evaluator applied the approved execution rights for
WO-PLG-012: start, implementation completion, and preparation of ready
VREC-PLG-009. Each writing operation obtained the real required GitHub
`validate` check for its exact HEAD. The start and completion receipts retain
the applied state changes; the [capture receipt](ready-record/vrec-plg009-prepared.json)
retains preparation. No extra owner approval prompt was needed for these
previously delegated rights.

VREC-PLG-009 is **ready**, prepared by `delegated-executor`, and binds clean
candidate `aea16859a3ea106e33f759098712cf80f83f3843`. Its 100 evidence files were
selected explicitly; links in an index were not recursively expanded. Raw linked
observations remain committed at the candidate. The record and normalized
evaluator sidecar belong to a later governance commit; candidate history is
preserved.

Technical readback confirmed the record's candidate/state and sidecar digest.
WO-PLG-012 remains implemented. This delivery did not verify a record,
release software, merge a PR or publish. The next record decision belongs to
the assurance owner.
