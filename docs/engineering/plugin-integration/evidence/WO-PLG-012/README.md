# Retained orientation and explicit briefing

The plugin copies preserve both existing helpers and contracts byte-for-byte.
Instructions resolve trusted plugin paths and a verified external evaluator.
Briefing is explicit; missing required current-state input recommends orientation
without running it. The existing canonical-output exception remains intact.

## Acceptance

- [Core comparison](retained-core-comparison.json) and [instruction review](instruction-review.md).
- [Actual helper tests](acceptance/helpers/REPORT.md): nine selected cases on each
  of Windows and Linux, with real denied-effect calibration probes.
- [Independent Windows instructions](acceptance/instructions/report.md): bounded
  C04/C05/C07 choices, protected UTF-8 bytes, existing inline schemas and refusals.
- [Eight fixed Linux calls](acceptance/linux-replay/REPORT.md): regression of the
  observer's selected commands; no new model-reasoning claim.
- C01ÃƒÂ¢Ã¢â€šÂ¬Ã¢â‚¬Å“C08 contain review copies of actions/stdout/stderr and observation indexes.
  Each index points to exact originals in short mapped retention paths. Original
  failed fixtures and observer outputs remain failed and visible beside corrections.

All observed target, ignored-file and Git hashes stayed unchanged. The calibrated
CPython boundary is test instrumentation. These checks do not qualify native
activation, production enforcement, live migration or prompt robustness.

## CI and lifecycle

Initial PR446 source CI passed 1,134 tests (four skipped). Released integrity,
graph and review preflight pass. Candidate doctor retains the six documented
0.18.0/managed 0.17.0 differences; managed roots were not changed.

The initial Windows upgrade rehearsal failed during checkout on inherited WO011 evidence
paths. Its dependent integration-package jobs were skipped. See
[actual CI results](checks/ci-initial/result.json) and the retained raw logs.
Passing source/validate checks do not cancel that failure. The operator subsequently approved WO-PLG-017; its completed repair is now
inherited from PR #449. All 56 relocated evidence files retain their original bytes.

The combined candidate 5aba5414 passed both upgrade rehearsals, but its
integration builder rejected the Git archive: 10,145 entries exceed the existing
10,000-member limit. The [failed CI result](checks/ci-member-limit/result.json)
and original logs remain retained. No completion was applied.

The correction packs this work order's 415 raw helper trace files into
[raw.zip](acceptance/helpers/raw.zip). Their bytes and flat names are unchanged.
The original REPORT, audit, inventory and source map stay directly readable and
byte-identical. The [raw index](acceptance/helpers/raw-index.json) binds every
ZIP member to the original inventory. Entries referenced by the original maps
resolve inside raw.zip; extract it into a separate temporary directory to inspect
or replay those logical paths. This does not rerun the helpers or change their
observed results. The [checker](../../../../../tests/plugin_integration/retained-skills/check_raw_archive.py)
compares the archive against the fixed original inventory.

[Independent archive review](checks/archive-review/REPORT.md) passes: 415 raw
payloads and four readable files are unchanged; both tamper cases are rejected.
The actual native Linux extraction guard accepts 9,816 entries at 6db8bd61,
using the unchanged 10,000-member limit. This is an extraction check; the complete
integration build and current full CI remain separate.

WO-PLG-012 is **implemented**. The released evaluator applied delegated completion
after all 13 checks passed at e152f6a8f30a62b4fb3676ada2e437f94544ee9c, including
Windows/Linux upgrade rehearsals and downstream integration. The successful
[corrected CI result](checks/ci-corrected/result.json) and
[actual completion receipt](governance/completion/wo012-completion-applied.json)
are retained. The plugin helpers, contracts and instructions are unchanged
from the accepted pre-repair candidate; see [byte readback](checks/parent-repair/core-readback.json).

The delegated route permits subsequent preparation of ready VREC-PLG-009 against
an exact clean candidate. Assurance, release, merge and live plugin installation
are separate; this implementation does not claim those effects.
