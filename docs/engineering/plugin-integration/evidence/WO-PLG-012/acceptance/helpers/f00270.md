The preparation identity probe omitted required --checkout-root and received
the actual RID005 refusal. This is a fixture runner argument error before init,
not a product failure. Original source and raw identity output are retained.
The retry supplies the new isolated output directory as its checkout boundary
and starts in a fresh attempt2 output.
