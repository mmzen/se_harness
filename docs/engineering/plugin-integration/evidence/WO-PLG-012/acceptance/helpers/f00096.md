The initial fixed process allowlist included --result-schema 2 for selected
check, but actual released0.16 help does not advertise that flag. The unchanged
helper correctly chose its existing no-flag check command, which the boundary
then refused. This is a fixture allowlist mismatch, not a product failure. The
allowlist is corrected to that exact existing read-only command; no helper or
evaluator code changed. Original argv/output/audit/source remains retained.
