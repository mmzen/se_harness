# Independent WO-PLG-012 instruction acceptance

The final copied briefing instructions pass the bounded Windows C04, ordinary C05 and C07 instruction observations. No candidate helper/product defect was observed. Exact-output behavior uses the existing canonical-output bypass; its supplemental checker result is not presented as a completed managed communication profile. Original static findings and observer failures remain retained.

## Scope and independence

The fixed oracle and raw source byte spans were written before reading any candidate plugin copies. Sources were approved SPEC/VER-PLG-012, REQ-PLG-020/021, retained managed skill contracts/helpers and TECHNICAL_COMMUNICATION. This pass writes only outside evidence files; no source repository, Git, artifact lifecycle or external action was changed. Each model invocation ran as one agent. Test orchestration with other verifiers is separate from the product skill procedure.

The accepted fixture identity uses parent-reviewed candidate SKILL hashes and pre-established unchanged managed helper/contract bytes, plus literal implicit=false YAML. Final brief SKILL SHA256 is `3e39fc61f7dfb41ca2b54d2c78f03ef2a7f3391402492dcd3ccef39a50a4af51`; unchanged helper is `b7a8a83d70f18cd8fd21ca0bc619131a80cc41413e84aa5ce3a4044c77f11683`. Correct retained canonical core digest is `ff2a0830ee08afd9f263bbcc0092c100b1682f05598adab2f9a168258bbbbf63`. Fixture trust does not qualify publisher trust, plugin distribution or native installation.

The actual interpreter was the externally installed released 0.16.0 environment, Windows11/Python3.14.6, isolated mode with bytecode disabled and cleared PYTHONPATH. Previously fixed archive identity was `a969d6ab9e80acc2c9f9e7b6679a02e7ffab371f1f11cb0c0f4243f208ed9eae`; payload identity was `51712fcfe5253db8d542deb870a0017b25c2976f5f4e76a81fd08923bba45e3c`. Actual direct identity returned both values and passed. Its target was a fresh local 0.16 fixture supplied by the portable verifier, outside any source checkout.

## Granular observations

| Case | Observed result | Exact records | Conclusion |
|---|---|---|---|
| C04 ordinary request | Model replied “The build produces a report.” No briefing/helper was selected. | model-observations/C04-routing | Pass; grouped model routing, native host activation unrun. |
| C04 helper control | Explicitly invalid skill value in otherwise valid input gave exit2/TCM003. | C04-direct-invalid-explicit | Pass; separate real checker test, not proof of activation by itself. |
| C05 ordinary Unicode | Version, identity, doctor then checker all exited0. Checker returned exact source digest and4 bindings. Non-ASCII occurs before and inside declared protected spans. ID, command, Unicode code and full assurance actor/conditions/qualification survived exactly. | C05-final-01-version through04-helper; C05-unicode-request.json; final-outputs/C05-* | Pass after observer receipt encoding correction; original failure files retained. |
| C05 canonical bypass | Model selected the retained exact-output bypass.270 exact bytes were copied and emitted alone, with source/output SHA `bb78722a28685df386cfbf5226c5dddc70e35bd6f1dda45b7cd60826f324d567`. No skill-result/receipt wrapper or managed-profile claim was emitted for this bypass. | C05-canonical-01-version through03-doctor; C05-canonical-operator-output.txt; model-observations/C05-canonical-bypass | Pass as higher-priority existing bypass; not a completed managed profile. |
| C05 canonical supplemental checker | Separate observer-requested unchanged helper probe gave exit0/one whole-source binding. | C05-canonical-supplemental-helper | Pass for byte-protection validation only; not selected by the bypass skill procedure. |
| C07 absent current state | After valid version/identity/doctor, model reported current-state-result-required/stopped and suggested orientation without invoking it. No inferred WO status, check, inspect, orient or brief-helper call. | C07-01-version through03-doctor; model-observations/C07; final-outputs/C07-* | Pass; actual model choice plus tool trace, not helper-inferred refusal. |
| Pre-exec version | Actual0.16.0 disagreed with supplied expected0.17.0; model stopped before helper. | preexec-version | Pass. |
| Pre-exec root | Direct identity refused checkout root: exit1, passed=false, RID003/RID004/RID010/RID024. Model stopped before helper. | preexec-root-version/identity | Behavioral refusal pass; original recorder incorrectly expected2 and retains passed=false. |
| Pre-exec payload | Direct identity refused all-zero expected payload: exit1/RID021. Model stopped before helper. | preexec-payload-version/identity | Pass. |
| Pre-exec paths | Model refused checkout-supplied and ambient relative helper paths, with no lookup/fallback/dispatch. | model-observations/preexecution-choices.json | Pass as two grouped instruction refusals. |

Ordinary Unicode source was502 UTF-8 bytes, SHA `66cedd5c2b34274a387fdac6d0db62b3b716b8531603fff0cd4b0ce60727ccb9`. Selected rendered bytes have SHA `58bd930030e9bc83fe337b4a3f6f42fb6ad3f8068a239b0c56fe84fe3290aed0`. Four source spans and their independent pre-render offsets/digests are in source-oracle.json; final rendered binding offsets are in the actual request/result. Additional operator/approval/clarification prompts were0 for every observed case. The normal Unicode procedure ran twice because the final static policy clarification arrived after the intermediate-core checker call; both source versions and actual sequences remain visible.

## Effects and boundary evidence

Every actual call retains argv, stdout, stderr, exit status, before/after full file maps, effective process bindings, audit events and executed source copies. All150 target files, including ignored content and every .git file, match across all call and model-choice intervals. The independent full-tree digest is `fbfb946139baaad1142a35d182de0a0ad85e5c1f3d3cbf75524a91c37e2c7bf6` (compact sorted path/SHA map, observer format). No lifecycle event, receipt or evidence file appeared in the target.

The independently supplied audit worker SHA `fff1982b67ef8945c1ff94876140fad23369ac55e6fbfd6b4b8fa2efa65cdb4d` was actually calibrated with denied write, socket creation, synthetic credential read, unexpected process and forbidden transition canaries. Each had one matching denied event and unchanged target hashes. Actual test calls had zero denied/network/credential/unexpected-process events, alongside allowed evaluator/engine/read-only Git process events. The latter are real subprocesses and are not hidden or called worker-free process execution. The candidate itself spawned no agent workers.

The boundary runs unchanged helpers and installed evaluator modules under a CPython audit wrapper and propagates that boundary to the actual required descendants. It is test instrumentation, not a production sandbox or native host proof. Native extension/Git internals are outside Python audit coverage; only fixed read-only Git vectors are admitted and all Git bytes are independently compared. Empty logs alone are not the claimed control evidence. C08's broader Windows/Linux helper/calibration pass belongs to the portable verifier; this report records the calibrated boundary actually used here.

## Retained findings and corrections

1. Original brief SKILL `25bfbb...` had inherited “Otherwise stop” ambiguity. Root statically clarified missing-current-state behavior in `6bb140...`. No behavior failure had been run against the original wording; original copies remain.
2. The original raw oracle's extra canonical case assumed a result/receipt wrapper around exact output. Reading the higher-priority retained TCM-PRT-005/PRF-004 resolved that as an observer expectation conflict: canonical-alone bypass takes precedence. Root clarified only copied SKILL prose, final `3e39fc...`. The original oracle remains unchanged; no schema/helper/policy change was invented to pass the case.
3. Observer package manifest serialization initially omitted the required final LF; provisional receipt core digests were wrong. Provisional snapshot digest formatting and pretty receipt JSON also did not use the retained canonical receipt representation. Original files/scripts remain; final-outputs contains the corrected existing canonical JSON bytes, including retained repository-byte and Git-reference manifest rules. Independent full-target snapshots and all raw candidate identity checks were valid throughout. This correction is output formatting, not a new helper execution.
4. The observer's first console emission failed on Windows cp1252 at the protected coffee symbol after both JSON files had been written outside the target. The original script/failure note remains; UTF-8 buffer emission corrected transport without changing rendered bytes.
5. Wrong-root direct identity was incorrectly given expected exit2 by the recorder (orient helper refusal uses2; direct identity uses1). Actual exit1/refusal/zero effects are retained. Installed cli.py's `_identity` independently confirms exit1. The original recorder result remains failed; it is not silently changed to passed.

## Coverage limits and replay

C04/C05/C07 are actual instruction observations in a shared independent verifier session with grouped fixed counterfactuals. They are not repeated fresh model samples, automatic host discovery, native-host invocation, or a claim of prompt robustness. No in-flight crash/interruption test was performed in this bounded instruction pass. No Linux instruction-reasoning claim is made here. Portable verifier replay of the fixed commands/requests is a command regression and must remain labeled as such. C01/C02/C03/C06 and broader C08 acceptance are separately owned.

Use final-outputs/C05-result.json and C05-receipt.json, C07-result.json and C07-receipt.json for final normal/stopped output objects; earlier root-level C05 receipt files are explicitly provisional failures/corrections. The canonical bypass uses only C05-canonical-operator-output.txt. trace-groups.json provides fixed command groups and request identities for replay. Evidence is flattened into numeric filenames with a SHA/source map so retention does not create deep repository-copy paths.
