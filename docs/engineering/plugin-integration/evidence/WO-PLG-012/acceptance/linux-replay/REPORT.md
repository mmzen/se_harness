# WO-PLG-012 additional Linux command replay

All eight frozen observer calls passed on the already calibrated native Linux
fixture with the final product core. C05 replayed actual version, identity,
doctor, and the exact Unicode helper request: four protected bindings and source
SHA25666cedd5c2b34274a387fdac6d0db62b3b716b8531603fff0cd4b0ce60727ccb9.
C04 replayed the exact invalid explicit-skill request and received exit2 TCM003.
C07 replayed version, identity and doctor only, then its fixed empty command span.
No helper checker or new state query was selected for C07.

These are fixed command regressions of the independent Windows observations,
not fresh Linux model activation, rendering, refusal, or prompt behavior. The
native helper/evaluator output, argv, source snapshots, original record hashes,
all target/Git/ignored-file hashes, and actual boundary events are retained.
All target hashes stayed unchanged and no unexpected/denied helper events arose.
Five real denied-effect Linux calibration records accompany this package.

The authoritative original C05/C07 result and receipt bytes are preserved with
the observer-confirmed canonical UTF8+LF hashes. Provisional original receipts
and manifest-correction evidence remain separately labeled. No Linux model
receipt or repaired original observation was synthesized.

The same CPython boundary limitations apply as in the main helper report: exact
read-only Git vectors and isolated config; no malicious native-code containment,
production sandbox, live external enforcement, lifecycle application, or native
host qualification. The main helper matrix is separate evidence and was not rerun.

Keep this package flat. source-path-map.json maps original paths to short retained
files; inventory.json hashes each retained payload. Duplicate identical bytes
share a retained file. All paths inside records remain original provenance.
