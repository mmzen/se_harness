```toml
artifact = "WO-PLG-012"
checkpoint = "handoff"
formal_snapshot_sha256 = "5a124b042b11747f15f32b2113fcd46cd5395cffb22cc4b8fd28d65541954ddd"
rebound_at = "2026-09-10T20:36:57Z"
```

# WO-PLG-012 handoff evidence

Retained by `harnessctl evidence`; body content is owner-authored.

## Implementation under test

The plugin now contains harness-orient and harness-operator-brief. Their helper
scripts and contracts are byte-identical to the managed originals. SKILL text
resolves the verified evaluator and trusted plugin helper paths; briefing stays
explicit and recommends, rather than automatically runs, orientation when
required current-state input is absent. Codex metadata disables implicit briefing.

Independent Windows/Linux helper and instruction cases are in progress. This
packet does not claim completed acceptance. Both skill checks, distribution
validation, released integrity, graph validation and review preflight pass.
The six documented candidate/released doctor differences are retained.

The real delegated start is retained in governance/start. WO-PLG-012 remains
in_progress. No completion, VREC preparation, assurance, release or merge has
occurred for this work order. Native activation and live migration are separate.
