# Owner instruction

The owner stated that the project is at a very early development stage with exactly one user, and requested KISS simplification.
They explicitly stated that old skill copies can be deleted and replaced by the plugin's skills.

After viewing `outputs/plugin-simplification.html`, the owner wrote:

> i accept this proposal, let's go you can create the artifact packet and work order (delegated route)

This packet implements that accepted direction, including the proposal's recommendation to use explicit checks instead of automatic blocking hooks.
The attached proposal preserves the wording shown before acceptance, including its then-pending product choice.
Acceptance selects that recommendation; it does not establish that implementation, verification, release or live adoption occurred.

The owner is the single accountable user. Any recorded packet approval identifies the corresponding requirements, technical, assurance-contract or engineering role.
Assurance-contract approval is agreement on the tests to require, not verification of an implementation.
