# Authorized API payload boundary

The representative inventory describes the inspected attempt-03 fixture, not
future bytes. Each launch retains its exact argv/prompt, settings, package and
loaded-script digests. The initialized repository and packaged skills are rebuilt
from the selected committed assets and released evaluator before attempt 04.

The existing isolated profile contains no `CLAUDE.md`, `settings.json`,
`settings.local.json`, `rules` or `agents` entry (metadata inspection on
2026-09-11). No ancestor `CLAUDE.md` exists above the inspected disposable repo.
Credential files were not read by that inspection. Normal CLI sign-in is used.

No-tools prompts request discovery/runtime/failure observations, a three-sentence
smoke-test explanation, resume acknowledgement, or `/compact`. The only Read
prompt selects the exact disposable `governed-target.txt`. Every edit prompt
states that the repository contains synthetic inputs, forbids lifecycle actions,
requires Read once followed by Write once on one exact disposable target with
`effect-CASE-NAME\n`, then requires stopping after success/refusal with no retry.
Available tools for edit cases are Read and Write; unsupported coverage uses Read
alone; no-tools cases expose no tool. Test faults operate on disposable plugin
copies and task-owned process fixtures. The exact reusable prompt templates are
in `tests/plugin_integration/claude_adapter/run_acceptance.py` and exact expanded
prompts are retained per launch in each case's actions and command capture.

Authorization permits these public/synthetic payloads and local test paths to
Anthropic's Claude API; product code and credentials remain excluded from prompts.
