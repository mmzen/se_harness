# Definition checkpoint selection

The generic handoff check rejected the approved WO with WEX210: its G3 work
authorization gate does not apply at handoff. The work has not started. Use
review preflight and Git-derived scope for this definition delivery, and the
start procedure after the approved definition reaches the configured base.
The rejected call changed no lifecycle state and is not reported as a pass.
