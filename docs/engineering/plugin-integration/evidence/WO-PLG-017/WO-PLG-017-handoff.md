```toml
artifact = "WO-PLG-017"
checkpoint = "handoff"
formal_snapshot_sha256 = "6c8ac29fd9dd955f0238a2b2f27c1377e10bcb5c0cc890b451813e72265fff55"
rebound_at = "2026-09-10T21:13:29Z"
```

# WO-PLG-017 handoff evidence

Retained by `harnessctl evidence`; body content is owner-authored.

## Definition delivery only

The operator explicitly approved WO-PLG-017 and authorized its bounded delegated
repair. The released evaluator applied only draft-to-approved. The exact plan
covers 55 native captures and preserves original file bytes/history. None has
moved. This PR introduces the approved definition to main; it does not deliver
the repair or authorize assurance, supersession, aggregate capture or merge.

The live delegated route cannot start until the approved execution class is at
the configured base. The next repository-owner action is to merge this definition
PR after its checks pass. WO-PLG-011 and WO-PLG-012 implementation PRs stay blocked
by the retained Windows checkout failure until the authorized repair succeeds.
