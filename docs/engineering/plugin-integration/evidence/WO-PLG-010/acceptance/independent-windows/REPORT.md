# Independent change-skill observations

The corrected skill completed the tested Windows instruction cases within the
limits below. This is an independent behavioral observation report, not a VREC
or a claim that VER-PLG-010 is fully satisfied. No live repository was edited.

The oracle and initial document/decision hashes were fixed before reading the
candidate. Tests used Python 3.14.6 with isolated released evaluator 0.16.0,
invoked outside all checkouts. Setup executed the existing skill's exact
PowerShell procedure against the independently fixed published wheel digest.
Current governance context came from real complete shared-handler output read
through its matching ending. This was tool-session delivery, not native plugin
host activation.

| Case | Observation |
| --- | --- |
| CHG01 | Created exactly SPEC-PKG-001, WO-PKG-001 and DEC-PKG-001 using released authoring commands. Supplied content hashes match. States remain draft, draft and open; graph validation passes. |
| CHG02 | Inspected changed ID, target state and actual content SHA-256 variants; invoked no affected transition and retained content drift. The three variants share one observer session. |
| CHG03 | Matching reviewed intent bytes produced one preview and one applied approval; no repeated owner prompt. Readback confirmed approved. |
| CHG04 | Corrected instructions continued an already started WO through two scoped edits and two local fixture commits. Scope, review preflight, validate, doctor and Git-derived handoff passed. No repeated approval/start or completion occurred; WO stays in_progress. |
| CHG05 | Refused the outside-path edit before invoking an editing tool. Prospective scope evaluation also returned QGP-G4I-PATHS. The outside file hash is unchanged. |
| CHG06 | Passing preflight, unrelated approval and an actor assertion supplied no start authority; no transition was invoked. These were grouped inputs at one unchanged state, not three fresh host sessions. |
| CHG07 | Not run by this observer: no network/credential access or live delegated start, completion and VREC preparation. A separate guard-only supplement cannot stand in for all three lifecycle applications. |
| CHG08 | Both create and transition receipts were suppressed after real process exit. Read existing files/history before continuing. No duplicate successful create, apply or owner prompt. These are injected receipt losses; an in-flight crash was not induced. |
| CHG09 | Applied only the exact WO approval; readback remained approved and the missing next right was DR-WO-START. An initial reserved-domain fixture defect was retained and repaired in a new fixture with a newly fixed input digest. |
| CHG10 | Assessed absent current delivery, stale delivery from another repository and a truncated payload without its ending; no mutation occurred. Exact setup reuse plus fresh complete context then allowed one covered WO approval with unchanged reviewed hash and no repeated prompt. Before/after checkpoint-free states are draft and approved. The unready variants were grouped at one unchanged state. |

Two observed instruction problems received narrow corrections and were retested:

- A definition recovery attempted `check --artifact INT-ACC-001`, which 0.16.0
  rejects with WEX210. The corrected skill uses file/history readback for
  definitions. The lost-transition-receipt repeat used that supported path.
- Applying `pre-action --procedure PROC-WO-IMPLEMENT` before initial edits
  failed QGP-G4I-EVIDENCE. Its exact corrective wrote a handoff packet, then
  the retry still required pre-action evidence. The corrected skill uses edit
  scope validation and the installed procedure's actual checkpoints. The two
  edits and commits then completed without manufacturing pending test success.

Raw commands retain argument arrays, status, stdout/stderr, and file inventories
before and after. `behavior-evidence/chg01.json` through `chg10.json` summarize
the cases. `summary.json`, `prompt-counts.json`, `candidate-identities.json`, and
`final-hashes.json` bind the observed scope. No duplicate owner-decision prompt
or interactive owner-decision tool call occurred in this evaluating session.

Original candidate bytes and corrected bytes are separately retained under
`candidate/` and `candidate-corrected/`. `trace-groups.json` describes the exact
recorded sequence for a separate Linux command replay. Such replay checks
platform command behavior; it does not constitute new independent model
reasoning or native skill invocation.

Do not retain the disposable Python environment or `.git` directories as
product evidence. Retain the report, fixed inputs, candidate copies, helpers,
raw artifact inputs and `behavior-evidence/`. The remaining acceptance gap is
the actual live delegated lifecycle route in CHG07, plus any separately
required native-host qualification. Linux results belong to the separate replay.
