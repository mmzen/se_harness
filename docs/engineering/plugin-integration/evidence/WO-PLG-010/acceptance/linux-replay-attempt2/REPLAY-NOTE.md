# Linux command replay result

The external released 0.16.0 evaluator completed the fixed Linux replay with
75 actual process calls: all 54 original recorded commands, plus fixture setup,
identity/help, corruption rejection and supplementary observations. The runtime
was Python 3.12.3 on Ubuntu 24.04 under WSL2. `summary.json` records exact runner
and fixture-manifest SHA-256 values; `behavior-evidence/` retains each actual
argv, exit status, stdout/stderr and target snapshots. `inspection.json` checks
that every original record was replayed and preserves the expected diagnostics.

Observed positive effects include one definition approval; one WO approval and
one separate start; two local feature commits within unchanged scope; draft
SPEC/WO and open DEC creation; and one applied transition followed by readback
with a single lifecycle event. The outside source hash remained unchanged.
The two original pre-action failures remain `QGP-G4I-EVIDENCE` observations;
the outside-scope call reports `QGP-G4I-PATHS` / `WEX201` without mutation.

The exact Linux Bash setup block installed the already available verified wheel
offline in private disposable data. The actual shared context handler delivered
the complete verified governance marker without changing target files. The
recorded covered WO approval sequence then applied once and read back approved.
As specified by the portable replay plan, lifecycle argv use the supplied
external platform evaluator; the separate setup/context path is also logged.

This is command regression, not a fresh model-behavior or native-host test.
CHG02/CHG06 and the unready CHG10 phase reconstruct the fixed operands and
already-selected empty mutation sequences, with unchanged hashes. They cannot
prove a new agent would make the same refusal. Decision-prompt counts remain
the original independent Windows observations. The lost-receipt cases replay
the underlying commands and recorded readback choices; receipt suppression was
injected after process exit in the original behavioral run. This replay does
not add a new model-facing delivery interruption. CHG07 is outside this run.

The previous run is retained separately with an explicit runner-defect note;
its incorrect commit-count assertion is not attributed to the candidate.
