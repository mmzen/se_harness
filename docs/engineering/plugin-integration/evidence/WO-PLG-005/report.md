# WO-PLG-005 implementation and acceptance report

**Operator acceptance recorded:** the operator accepted hook failure as a [documented local limitation](local-limitation-acceptance/README.md). The failed observations remain unchanged. Formal decision recording and any required contract changes are separate; this evidence update changes no lifecycle state.

WO-PLG-005 remains **in_progress** and **unqualified** under VER-PLG-005. Current evidence supports C01–C09 on the routes stated below and C12's rejection of invalid configurations. **C10 and C11 observed failed enforcement:** timeout, missing output, invalid output and loss of the active binding each allowed an exact synthetic edit after ordinary one-time file approval, without a supported handler refusal. Literal OS shell-start failure remains unavailable. The [qualification limit](qualification-limit.md) records these findings and their scope. A passing observation does not qualify the whole adapter or approve completion, verification, release or publication.

The [case index](#acceptance-status) links each conclusion to its retained captures. Each `Cnn/observations.json` is a consolidation with expected/observed results and source digests. The adjacent `actions.txt`, `stdout.txt` and `stderr.txt` are explicitly labeled indexes, not invented command runs or replacement raw output. Original nested attempts remain unchanged.

## Implementation and identity

`plugins/verity-plane/codex/` contains the native manifest, generated synchronous inline PowerShell guards, and the Codex dispatcher. The guards reject unsupported profile/decision selections before invoking Python, preserve an independently registered event for malformed-input refusal, and give explicit missing-runtime guidance. The dispatcher calls the unchanged shared handlers with the absolute selected Python, `-I -B`, released 0.16.0 identities and fixed context/timing bounds. It returns only the supported deny/context envelopes and rejects empty context, authority-granting decisions and unknown fields. Unsupported tools receive an explicit coverage-gap message.

The profile is confined to DEC-PLG-001's Codex 0.153.4, Windows, Python 3.14.6 and evaluator 0.16.0. Profile labels and selected decision fields are eligibility inputs, not evidence of a running host or new artifact authority. `preparation/accepted-profile/identity.json` retains the selected real executable/version/digest. The root's governing released evaluator remains 0.17.0; neither shared components nor approved SPEC/VER/DEC definitions were amended.

`tests/plugin_integration/codex_adapter/` contains focused transport and observer tests, disposable repository/package preparation and the read-only app-server observer. That observer uses the accepted process observer, refuses every incoming server request without answering it, and requires exact package/cache and enabled/trusted binding identities before ordinary thread work. It has no edit or setup mode. A separate, parent-reviewed `edit_observer.py` accepts at most one exact predeclared synthetic update after independent path, patch, protocol and permission checks; its default mode is preflight only. Actual permitted, refused and Unicode-path edit observations are retained below. The loaded-payload comparison excludes no files. Dedicated C05/C08/C09/C12 runners select reviewed disposable fault inputs; they do not change the production package or grant lifecycle authority. The prior process-local plugin-disable attempt did not disable the old probe hooks; native review subsequently disabled those two hooks, as preserved in the incident record.

The checked historical package selects committed source `ab8d6e2049f87a2d5f1820590f86dfaafdf40416` and bundles the released 0.16 wheel. `preparation/package-02/package.json` retains its archive digest and every payload digest. The unchanged assembler's build and independent check passed; native marketplace/plugin installation succeeded. That package predates the guard's pre-Python eligibility fix and cannot prove qualification of the changed adapter. Its later replacement is recorded below.

The subsequent `preparation/package-03/` build and independent check passed for source `c7d30a92dab5d3fb12c6e42fd3d8ae9ed6ce2163`, producing archive `effa69a1eb7f50a7cb1eda38bd955083886a327b6d9b6eaa3c75e79068707a53`. Its preparation record retains the exact proposed update and one-hook review/stop procedure. The parent subsequently reviewed and executed that source update/native reinstall, retained in `preparation/native-update-02/`, then trusted only its SessionStart command and externally stopped its owned job with zero remaining active processes, retained in `preparation/trust-session-start-01/`. No setup action occurred in that later bounded review; the earlier incident's unknown historical effects remain unresolved. PreToolUse trust and qualification paused for the path defect below; this partial trust cannot qualify subsequently corrected bytes.

## Checks and retained failures

The [final local check run](checks/final-live-checks-01/results.json) records **143 focused tests passing**, passing candidate CLI help, graph and release-distribution checks, and passing isolated released 0.17 validation, doctor and WO-PLG-005 preflight. Its [source snapshot](checks/final-live-checks-01/source-inputs.json), commands and output hashes identify what ran. Candidate-source doctor exits 1 for exactly the six known distribution-template differences; the governing released doctor passes. These local checks do not resolve C10/C11 enforcement failures or qualify the unavailable shell-start case. Later exact-head CI and evaluator handoff remain separate.

The explicit focused command is the external 0.16 environment's Python with `-I -B -m unittest discover -s tests/plugin_integration/codex_adapter -p test_*.py -v`. `checks/focused-interim-01/` records seven passing tests and `checks/focused-interim-02/` records eleven passing tests; `checks/focused-interim-03/` records twelve passing tests after the payload comparison was added. Top-level discovery alone does not discover these tests. These are boundary/calibration checks, not substitutes for the real-host scenarios below. Parent-coordinated broad checks and CI remain separate.

Released evaluator 0.17.0 generated the handoff evidence packet and passed the handoff checkpoint against `origin/main` at `78cd64df7c130a6f42ab357836093db30f710753`. `checks/interim-handoff-01/` retains both commands and outputs; the bound formal snapshot is `adcc15b6639b67810dd01e44d67204feeef49a4051759c6d659a6678aadf1716`. This checkpoint does not complete the work or qualify unrun cases. Its separate delegated-completion gate lookup was unavailable under the restricted network; no completion decision was attempted. Evidence ordering is the actual interim handoff ordering, not a claim about the initial edits.

The first package attempt used a forbidden destination for the dispatcher. Its failure is preserved in `preparation/package-01/`; the corrected existing `scripts/` destination is in the successful later package. The initial pre-action checkpoint ordering deviation, original failed path arguments and passing correction are preserved in `checks/pre-action-after-initial-edits/`. No checkpoint is falsely described as preceding the initial edits.

The generic Plugin Creator validator could not start with either the selected external Python or the bundled authoring Python because both lacked PyYAML. Both actual failures are in `checks/generic-plugin-validator-01/` and `-02/`; no package installation was attempted to repair those runtimes. Static inspection of the retained validator identity's `validate_manifest_shape` allowlist shows it also excludes the accepted native `hooks` key. This is a generic-authoring-validator limitation, not a native-host validation pass. The parent explicitly retained DEC-PLG-001's native route; the accepted key was not removed to satisfy a different validator.

After the interim commit, the parent authorized only a bounded read-only app-server inventory. `C01/stale-inventory-01/` retains an observer preparation failure on the misspelled `utf8-sig` codec alias, before the first native command. The correction uses `utf-8-sig` and a focused test covering UTF-8 with and without a BOM. `C01/stale-inventory-02/` retains the subsequent real native inventory: both historical plugin hooks were enabled/trusted, both old probe hooks disabled, and the three expected shared skills resolved. The entire historical loaded cache matched the checked historical package. The observer then stopped on the current-candidate mismatch; only initialize/initialized/hooks-list/skills-list were sent, targets remained unchanged, and owned Job Object cleanup reported zero active processes. This is partial historical discovery evidence, not a passing current-candidate C01 or C02.

`checks/focused-interim-04/` records the subsequent fourteen-test focused run, including BOM preparation and external stop/deadline cleanup boundaries. The revised `interactive.py` was then prepared for separate review: a fresh external marker or a fixed 180-second deadline stops its owned job without any menu exit navigation. It had not run at that checkpoint. A fresh source-manifest version was generated using the Plugin Creator cachebuster helper after validating the existing disposable marketplace name; it enables a later native reinstall to obtain a distinct cache path, not a support-profile change or installation authority.

The parent's bounded SessionStart review later exercised that external marker and job cleanup as described above. A direct registered-shell calibration then confirmed that the original `.NET IsPathRooted` check accepted drive-relative and root-relative environment selections and invoked the sentinel Python dispatcher before later validation. `checks/native-path-before-fix/` preserves both failures. The minimal guard correction validates a fully qualified drive or share root before any ancestor lookup/interpreter launch; existing traversal/reparse checks remain. `checks/native-path-after-fix/` repeats the exact inputs and records valid denials with no sentinel invocation. Both native-path directories now retain original stdout bytes in `stdout.raw-bytes.json`, with filename/hash mappings and README labels. Their `stdout.txt` files are separately labeled readable projections; original double-CR sequences remain losslessly preserved in the containers. Actions, observations and stderr were not changed. `checks/native-path-format-verification.json` verifies both original hashes and records a passing whole-branch whitespace check with the established `core.whitespace=cr-at-eol` setting. `checks/focused-interim-05/` records fifteen passing tests, including negative environment and repository path selection and the existing positive Unicode/quoted-path case. The fresh default cachebuster identifies corrected command bytes; a fresh checked package and native trust are still required.

`preparation/package-04/` contains that subsequently built and independently checked package from source `400cf3d5d669e239d046cc640ac94e962a6e731d`, archive `a94185033507c3e3c65de890a6979c34048e6905ea6062a270d004b1b254c4c5`. The parent reviewed and executed the bounded update (`preparation/native-update-03/`) and separately trusted each changed command (`trust-pre-tool-use-02/`, `trust-session-start-02/`). Both jobs were externally stopped with zero active processes; no menu-exit navigation, model prompt or setup action occurred in those later cycles. The earlier incident's unknown historical effects remain unresolved.

`C01/current-inventory-01/` proves that the entire actual loaded cache matches package-04, both required commands are enabled/trusted/synchronous with 30-second timeouts, and the three shared skills resolve to the expected cache paths. The old probe hooks remain disabled. The inventory sent no thread/model request and cleaned up its owned job. A later assessment serialization corrupted the Unicode target-name key; the prior serialized observation is retained separately and the documented correction changes only that key, preserving original hashes/bytes.

`C02/startup-01/` records the real startup hook, exact absolute `-I -B` argv, successful released identity/doctor checks, and the host's full 7,901-byte context receipt. The 7,567-byte gate/router body matches the independently prepared sources and end digest `5b1ed562a275f5ed51a14a632b0b9a3779daebb4165e1f908db33f8241541ca2`. Native hook duration was 2,095 ms; the job had zero active processes after cleanup and target hashes were unchanged. `C02/assess_context.py` reproduces the retained-receipt comparison.

The first resume/compaction request was rejected by automatic approval review **before process creation**, because sending retained governance context, file contents and local paths to the Codex service required explicit payload/destination consent. [C02/resume-compact-01](C02/resume-compact-01/observations.json) retains that rejection; it was not bypassed. The [payload inventory](preparation/service-payload-review/inventory.json) bounds the public/synthetic fixture material and local paths, without reading credential contents or claiming to be a captured API request. The operator subsequently [authorized the remaining bounded live tests](preparation/service-payload-review/operator-authorization.json). That consent permits the specified public/synthetic payload to reach the OpenAI Codex service; it grants no new host-security permission or lifecycle decision.

`C04/registered-shell-01/` and `C07/registered-shell-01/` retain exact native command arrays, raw inputs, stdout/stderr, responses, positive sentinel controls and actual helper-spawn logs. Six malformed/required-field negatives and ten profile/decision negatives pass; every negative leaves the sentinel log unchanged and the real lifecycle artifact hashes remain unchanged. These are local registered-shell cases, not pending native-edit observations.

`checks/focused-interim-06/` records **34 passing tests in 11.196 seconds**, including the separately reviewed edit observer's 19 tests. `C03/observer-preparation/` retains the parent's concrete one-edit plan, no-host preflight, review note and source hashes. `C08/runtime-preparation/` retains offline creation of an owned disposable 0.16.0 runtime and its passing full isolated identity; this enables literal interpreter-removal testing without touching the shared evaluator environment. No C03/C08 native operation is implied by these preparations.

The small repository checks in `checks/required-interim-01/` pass candidate CLI help, candidate graph validation, release-distribution validation, and isolated released 0.17 doctor/preflight. Candidate-source doctor exits 1 only for six candidate-versus-released template differences (`.engineering-harness.toml`, the managed workflow, router, authoring policy, traceability policy and WO template); no root managed file was changed. The governing released doctor passes. `checks/parent-ci-9d536b3d/` retains the parent's 17 passing CI checks for earlier exact head `9d536b3de6adcb91ddfd22d3384980744d5d05e8`; it is not a CI claim for this later evidence/observer commit. The subsequent evidence-format cleanup retains every original parent CI log byte in a base64 JSON container with SHA-256. `log-byte-mapping.json` maps original filenames/hashes to lossless containers and separately labeled readable `.log` projections; projection whitespace/newline normalization does not change the retained raw evidence. Original commands and index metadata remain unchanged. The earlier whitespace-check failure remains historical evidence.

The refreshed released 0.17 evidence/handoff checkpoint in `checks/interim-handoff-03/` passes against `origin/main` with formal snapshot `adcc15b6639b67810dd01e44d67204feeef49a4051759c6d659a6678aadf1716`. It leaves WO-PLG-005 in progress. The separate delegated-completion gate lookup remains unavailable under the restricted network; no completion or verification decision was attempted. The packet's owner-authored body points to this incomplete acceptance report.

## Acceptance status

These are observation results for the selected Windows profile, not assurance decisions. No absent output, unchanged target alone, or observer cancellation is interpreted as hook enforcement. C04/C07 use the accepted registered-shell test route. C12 inspects actual native configuration and rejects qualification without executing an invalid hook.

| Case | Current result and direct evidence | Limit or remaining evidence |
| --- | --- | --- |
| [C01](C01/observations.json) | **Pass:** [current discovery](C01/current-inventory-01/observations.json), full package/cache equality and setup/change/evidence skill resolution | Discovery only; enforcement is assessed separately |
| [C02](C02/observations.json) | **Pass:** [startup context](C02/startup-01/context-assessment.json), [resume and fresh context](C02/resume-compact-03/context-assessment.json), [completed compaction/restoration](C02/resume-compact-03/restoration-assessment.json) | Exact complete receipts on this profile; earlier rejected/failed attempts remain historical |
| [C03](C03/observations.json) | **Pass:** [permitted edit](C03/permitted-edit-02/independent-assessment.json) and [refused edit](C03/refused-edit-01/independent-assessment.json) | Refusal is proved by native blocked status, matching evaluator denial and unchanged hashes; no raw refused patch was emitted |
| [C04](C04/observations.json) | **Pass on registered-shell route:** [six malformed/required-field negatives](C04/registered-shell-01/observations.json), raw responses and positive sentinel controls | No native pending-edit claim |
| [C05](C05/observations.json) | **Pass:** [disabled required SessionStart](C05/disabled-session-start-01/observations.json) and [actual shared-script logical failure](C08/wrong017-01/live/dispatch.jsonl) | The shared script returns UNREADY at exit 0 after identity exits 1; this is not a crashed shared script |
| [C06](C06/observations.json) | **Pass:** [supported Unicode/quoted path](C06/unicode-edit-01/native-assessment.json) and [unsupported tool warning](C06/unsupported-noop-01/native-assessment.json) | Unsupported command reached the hook; ordinary command approval was unanswered and the command did not execute |
| [C07](C07/observations.json) | **Pass on registered-shell route:** [ten negative profile/decision selections](C07/registered-shell-01/observations.json), unchanged lifecycle hashes and helper log | No broader profile support or lifecycle decision |
| [C08](C08/observations.json) | **Pass:** [absent before setup](C08/missing-01/observations.json), [ready owned runtime](C08/runtime-ready-01/observations.json), [literal removal](C08/removed-01/observations.json), [wrong evaluator identity](C08/wrong017-01/observations.json) | Invocation audit concerns the selected absolute interpreter; no claim about unrelated Python processes |
| [C09](C09/observations.json) | **Pass:** [failed](C09/failed-01/observations.json), [interrupted](C09/interrupted-01/observations.json), [stalled](C09/stalled-01/observations.json) evaluator children; native denial in 2.284/2.238/8.831 seconds | Instrumented fault runtime only; final targets unchanged, inner cleanup before response, outer jobs empty and inputs restored |
| [C10](C10/observations.json) | **Failed enforcement, observation complete:** [host timeout](C10/host-timeout-01/observations.json) after 30,012 ms; one exact synthetic file update follows ordinary one-time approval | No supported handler refusal; the route remains unqualified. Final targets match the observed effect; configuration, binding and runtime are restored and outer cleanup is zero |
| [C11](C11/observations.json) | **Failed enforcement:** [missing output](C11/missing-output-01/observations.json), [invalid output](C11/invalid-output-01/observations.json), [binding lost after readiness](C11/lost-active-binding-01/observations.json); one exact approved update in each | Three negative observations complete. Literal OS shell-start failure remains unavailable; binding loss is observed across a fresh host/session, not hot reload or literal file deletion |
| [C12](C12/observations.json) | **Pass, rejection only:** [one-second timeout](C12/timeout-1-native-01/negative-assessment.json) and [asynchronous binding](C12/async-true-native-01/negative-assessment.json) | Both appeared as modified/untrusted definitions; no thread, hook execution, edit or new trust approval was attempted |

## Current native observations

**C02 restoration.** [resume-compact-02](C02/resume-compact-02/restoration-assessment.json) delivered the complete resumed context, but requested restoration after the compaction item completed and before its turn completed. The host rejected the premature request with `ActiveTurnNotSteerable`. The narrow observer correction waits for the same successful compaction turn. [resume-compact-03](C02/resume-compact-03/restoration-assessment.json) then received complete 7,901-byte contexts on both the resumed and restored turns, with exact dispatcher output and argv. Actual compact-turn completion precedes restoration; the restored turn completes, sentinel hashes remain unchanged and owned-process cleanup reports zero active processes. A model reply of READY alone is not the proof of context delivery.

**C03 mapped edits.** [permitted-edit-01](C03/permitted-edit-01/observations.json) stopped before approval because the native patch proposed LF replacement bytes while the reviewed plan required CRLF. The separate [permitted-edit-02](C03/permitted-edit-02/independent-assessment.json) used an explicitly reviewed LF replacement: the native checked hook preceded one exact correlated acceptance and completed file change, with the expected final hash. [refused-edit-01](C03/refused-edit-01/independent-assessment.json) recorded the actual out-of-scope path in evaluator argv/result, `WEX201`, a matching native blocked hook in 2,499 ms, no approval response and no changed sentinel. The original observer called that attempt unavailable because a blocked native action emitted no fileChange item; the independent assessment explains the terminal limitation without rewriting the capture. The attempted replacement bytes are not inferred from the prompt when no native patch is exposed.

**C05 missing binding and logical failure.** [disabled-session-start-01](C05/disabled-session-start-01/observations.json) observes the required SessionStart binding disabled, its original command trust preserved, no session-context dispatch or receipt, and no target change. Normal inventory validation rejects the missing binding. The runner restores the exact original configuration and verifies native active inventory. The second subcase reuses [C08/wrong017-01](C08/wrong017-01/observations.json): the native dispatcher actually invokes the unchanged `session-context.py`; the real identity subprocess exits 1 with RID002/RID021/RID022, and the shared script returns an UNREADY context at exit 0. That context reaches the native hook. This is a logical shared-script failure, not a dispatcher-only rejection or process crash.

**C06 argument boundaries and coverage.** [unicode-edit-01](C06/unicode-edit-01/native-assessment.json) binds the exact `café quoted ' target.txt` path through the native patch, evaluator arguments and final bytes. The earlier [unsupported-plan-01](C06/unsupported-plan-01/observations.json) exposed no available plan tool and is not counted. [unsupported-noop-01](C06/unsupported-noop-01/native-assessment.json) instead reached an actual unsupported command hook and received the explicit unenforced-coverage warning. The observer stopped at ordinary command approval without answering it. This demonstrates warning delivery, not command execution or shell enforcement.

**C08 prerequisites.** A fresh owned 0.16 runtime is prepared offline and verified before the literal-removal subcase. The before-setup and removed-runtime native receipts give setup-required guidance without a dispatch through the missing interpreter. The ready control delivers complete context; the wrong 0.17 environment invokes the shared handler and fails its full identity check. Each run retains actual binding inputs, source digests, dispatch audit, native receipt, target hashes and restoration. No shared evaluator environment was removed.

**C09 evaluator faults.** The three real mapped-edit runs select only the reviewed final evaluator-check invocation in a separately owned, explicitly instrumented runtime. Identity, doctor and full startup context pass before fault injection. The failed child exits 3; the interrupted child exits 15 after its exact PID, creation time and owned-job membership are checked; the stalled child reaches the 8-second inner deadline. Each shared denial matches the native synchronous blocked receipt, received in 2.284, 2.238 and 8.831 seconds respectively, before the 30-second host timeout. Evaluator descendants reach zero before the response. Independent final snapshots show unchanged targets, zero completed file changes, no acceptance, zero outer-job processes and exact binding/runtime restoration with the fault configuration removed. Reassessment reproduces all three results and separately checks these final gates. Original observer exit 1 and its no-fileChange-after-blocked terminal limitation remain visible. Optional `native-shells.json` records shell metadata; it is not the basis of the enforcement result. These observations neither qualify the instrumented runtime nor establish a global write-count guarantee.

**C10 host timeout.** [host-timeout-01](C10/host-timeout-01/observations.json) stalls the selected dispatcher at startup, before the shared handler or its inner deadline starts. The production guard and 30-second native binding remain unchanged. A complete 7,901-byte SessionStart context arrives first. The native before-tool hook then reports `failed` and `hook timed out after 30s`, with duration 30,012 ms and an independently observed interval of 30.046 seconds. The host subsequently requests ordinary approval for the one reviewed synthetic patch; the unchanged one-edit observer accepts that exact patch once, and a completed native file update and its final SHA-256 confirm the effect. No supported handler refusal was observed. This is a completed negative observation and failed enforcement, not a passing route. The final configuration and binding match their original bytes, the fault configuration is removed, the owned runtime inventory is restored and the outer job contains zero processes. The naturally returning 90-second fault path was not reached. The retained changed target is an observed effect, not a restoration failure; the other sentinels remain unchanged.

**C11 output and binding failures.** [missing-output-01](C11/missing-output-01/observations.json) returns zero bytes at exit 0; the native hook reports `completed` with no entries. [invalid-output-01](C11/invalid-output-01/observations.json) returns truncated JSON at exit 0; the native hook reports `failed` with an invalid-output diagnostic. Both report 575 ms, with observed intervals of 0.592 and 0.594 seconds respectively. Each proceeds to ordinary one-time approval and one exact completed file update, with no supported handler refusal. Their actual input, output bytes, request/approval, native item and final target hashes agree. Configuration, binding and runtime restoration and zero owned processes are confirmed.

[lost-active-binding-01](C11/lost-active-binding-01/observations.json) first completes a real ready activation, then disables only the PreToolUse binding while retaining its trust hash. A fresh host/session sees that binding disabled and SessionStart enabled; normal inventory validation rejects the missing required hook. A comparison-only copy permits checking the remaining command/payload fields while the retained native inventory stays unchanged. Full startup context arrives, but no PreToolUse hook or shared tool check runs. One exact approved file update follows. The original configuration and active native inventory are restored, all three phase jobs end empty, and final hashes match the recorded effect. This demonstrates binding loss across restart, not same-process hot reload, literal guard-file deletion or OS shell-start failure. The [initial preparation capture failure](C11/binding-loss-preparation-01/preparation-status.json), caused by a Windows-encoded preview rejected by a UTF-8 decoder, remains historical; it occurred before native execution or profile changes. The corrected preparation uses ASCII-escaped JSON.

**C12 invalid configuration.** Native `hooks/list` exposed `timeoutSec=1` in one disposable variant and `async=true` in the other. The former cannot exceed the 18-second inner/startup/cleanup/output budget; the latter violates synchronous before-tool enforcement. Both changed the native PreToolUse `currentHash` and had `trustStatus=modified`; SessionStart remained trusted. No new command trust, model turn or effect was requested. This is rejected qualification of observed configurations, not evidence that either invalid hook ran or prevented an effect. The [timeout restoration](C12/timeout-1-native-01/outcome.json) and [async restoration](C12/async-true-native-01/outcome.json) confirm the original marketplace bytes, selected native version, entire payload, binding hashes, exact configuration and sentinel hashes. Native cache contents were changed only by native installation, never edited directly.

**Remaining qualification limit.** Literal OS shell-start failure is unavailable in the current evidence; it was not emulated or waived. Dispatcher output injection and disabled-binding observations establish their named conditions only. The observed C10/C11 effects have no supported handler refusal or independent blocking control. These failed enforcement results remain unresolved; recording them accurately cannot qualify the route.

## Native menu incident and boundary

`preparation/native-menu-deviation-01/README.md` records the actual sequence: batched navigation unintentionally selected Windows sandbox setup; Ctrl-C displayed shutdown, but a later scoped observation found the owned process still active and two newly created, empty disposable sandbox directories. Parent-authorized containment revalidated the full known ancestry and stopped only the exact owned Codex process; its helper and shell exited, and the PTY completed with exit status 1.

The historical still-active observation remains intact. No elevation approval was accepted, but the evidence does **not** establish whether a short-lived/detached setup helper or any operating-system security change occurred. No absent-OS-effect claim, silent undo, or settings alteration is made. No acceptance case passes because of that interaction. Further UI/setup/security operations are not authorized by this report. Later app-server and exact synthetic-edit tests have their own bounded review and operator payload consent; they do not erase the incident or expand host permissions.

Exact-candidate checks remain separate. C10/C11 failed enforcement and the unavailable shell-start case must remain visible in the resulting handoff. This report leaves lifecycle state unchanged. It does not turn earlier CI, a passed checkpoint, or a completed negative observation into completion or assurance verification.
