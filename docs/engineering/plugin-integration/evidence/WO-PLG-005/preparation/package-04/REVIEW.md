# Corrected absolute-path guard: proposed refresh

The before/after calibration in `../../checks/native-path-before-fix/` and `native-path-after-fix/` confirmed and corrected incomplete Windows path selection before Python. The minimal source change adds a fully qualified root check before filesystem ancestor inspection or interpreter launch. Existing traversal and reparse checks remain; the fifteen focused tests include both refused incomplete paths and successful absolute Unicode/quoted paths. No additional host/profile qualification is inferred.

This package was built and independently checked from source `400cf3d5d669e239d046cc640ac94e962a6e731d`. Version is `0.0.1+codex.20260911195218`; archive SHA-256 is `a94185033507c3e3c65de890a6979c34048e6905ea6062a270d004b1b254c4c5`; hooks file SHA-256 is `2b768288dd11dca1510ea250e0b1cf9c7455e1f6d9152e62ed45482336c75a43`. Exact payload hashes, literal command bytes and their independent text hashes are in `package.json` and `review-plan.json`. Native trust hashes must still be observed and are not inferred from text digests.

`proposed-update.py.txt` is review source, not an executed update. It now requires the current source marketplace **and actual native cache** to match package-03's inventory before copying the same named files from package-04. It rejects linked source/target/cache ancestry. The native CLI argv remain the reviewed `plugin add` then `plugin list` under the existing disposable environment. New execution evidence would go to `native-update-03/`. No cache/marketplace-metadata/credential/security edit or rollback is included.

Both inline command hashes changed, so neither the earlier SessionStart trust nor the unfinished PreToolUse review qualifies these bytes. The same reviewed one-hook-per-process procedure in `../package-03/REVIEW.md` applies using only the **new** `trust_reviews` entries in this `review-plan.json`: fresh stop markers, fresh `trust-session-start-02` and `trust-pre-tool-use-02` evidence folders, external marker immediately after the one trust action, fixed 180-second deadline and owned-job zero-active-process confirmation. The helper source is unchanged from the parent's successful marker-stop trial. No Escape, `/quit`, setup prompt confirmation, security change, or model operation is part of this refresh.

The parent performs/reviews any approved native refresh and UI action. After both commands are actually trusted, a separate read-only inventory must match the entire checked package and active synchronous bindings before C02 thread work. Earlier unknown operating-system effects remain unresolved.
