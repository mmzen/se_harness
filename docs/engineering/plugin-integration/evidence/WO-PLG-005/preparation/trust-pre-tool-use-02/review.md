# Corrected PreToolUse command trust

The parent reviewed package-04's guard, literal commands, payload inventory and
updater before executing that exact updater. Native `plugin add` and `plugin
list` completed; captures are in `../native-update-03/`.

The bounded UI helper used its reviewed read-only/on-request argv. After the
terminal-capability warning, the parent selected **Review hooks**, opened
**PreToolUse**, moved from the disabled historical probe hook to the changed
`verity-plane@verity-plane-codex-fixture` hook, and inspected its synchronous
mode and 30-second timeout. Each input was sent separately and its returned
screen inspected. Pressing `t` changed that one hook to enabled and **Trusted**.

The parent immediately created the reviewed external stop marker instead of
navigating away. `outcome.json` records PID 29392 and zero active processes in
the owned Windows Job Object; the PTY exited 0. No menu-exit command, Windows
setup action or model prompt was sent. The unavailable `codex_apps` connection
was not repaired or reconfigured. These are observer transcriptions and process
records, not byte-exact terminal captures or native acceptance results.
