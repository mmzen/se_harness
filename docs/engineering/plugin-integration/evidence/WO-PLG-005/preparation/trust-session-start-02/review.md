# Corrected SessionStart command trust

After the corrected PreToolUse review process had stopped, the parent launched
the separate bounded helper for SessionStart. The UI reported one changed
hook. The parent selected **Review hooks**, opened the selected SessionStart
category, then selected the changed `verity-plane@verity-plane-codex-fixture`
hook. The historical probe hook remained disabled. The parent inspected the
startup/resume/clear/compact matcher, synchronous mode and 30-second timeout,
then pressed `t`; the selected hook became enabled and **Trusted**.

Each input was followed by inspection of the returned screen. The parent then
created the reviewed external stop marker. `outcome.json` records PID 32868 and
zero active processes in its owned Windows Job Object; the PTY exited 0. No
Escape, `/quit`, menu-exit Enter, Windows setup action or model prompt was sent.
The unavailable `codex_apps` connection was not reconfigured.

These transcribed observations concern the exact package-04 commands. A separate
native inventory and actual delivery observations are required for C01/C02.
The earlier incident's unknown historical operating-system effects remain
unresolved; these two correctly contained reviews do not retroactively settle it.
