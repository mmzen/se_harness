```toml
artifact = "WO-PLG-020"
checkpoint = "handoff"
formal_snapshot_sha256 = "9fafd34d95bb96416bd1cbc5ca9e9754dba5df0632c23393f46d1c5becb9ced1"
rebound_at = "2026-09-12T14:57:25Z"
```

# WO-PLG-020 handoff evidence

Retained by `harnessctl evidence`; body content is owner-authored.

## Implementation review

Prepared by the delegated executor for the approved WO-PLG-020 scope. The work
order remains `in_progress`; this packet does not record completion or assurance.

The candidate supplies explicit plan/apply migration and restoration for the
seven retained repository skill files. A closed schema-4 portable binding,
shared inventory checks, exclusive transaction, and bounded recovery journal
preserve ownership through supported installer and integrity entry points.
Default repository ownership continues to use schema 3. Initial migration and
rebinding validate external assembly data without executing plugin content.

## Evidence and remaining acceptance

The [evidence index](README.md) retains the operator's packet and supplemental
approvals, start receipts, development inputs, failures, repairs, and limitations.
Local source and earlier frozen-package fault runs completed with 40 passed case
records plus one unavailable symlink case each; their 85 exception boundaries
and 67 precommit process-termination boundaries passed. The source old-reader
probe was corrected and rerun against actual released 0.17.0. These remain
development observations and do not qualify one immutable implementation commit.

After supplement revision 2, 39 CLI/documentation checks and 38 CI contract checks
pass. Embedded Python compilation and all 13 extracted PowerShell script parses
pass. Released-evaluator preflight, doctor, graph validation, and scope checks
pass; release-distribution validation reports 14 passing distribution-bearing
records. The first canonical full-source run is retained as failed; the repaired
candidate still requires its frozen full-source run and hosted acceptance.

Required hosted acceptance uses the one committed-candidate wheel on Ubuntu and
Windows: Python 3.11 source/package smoke and Python 3.13 full ownership/fault
coverage. Completion requires those observations and the live successful
`validate` check for the exact candidate. VREC-PLG-015 remains unprepared.

## Boundaries

The root remains governed by released 0.17.0 and has not been migrated. No plugin
installation, native-host activation, C10/C11 enforcement improvement, release,
or WO-PLG-009 connection is claimed. Existing binding compatibility and external
provider availability are separate observations. Missing and unsupported
filesystem observations remain explicit in their original records.
