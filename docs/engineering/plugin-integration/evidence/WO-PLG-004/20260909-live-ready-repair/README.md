# Incomplete readiness trial retained

Real host tools denied the exact sentinel, created the external venv, and installed the exact released wheel offline. A subsequent SessionStart actually ran identity, doctor, preflight and check; preflight reported ready:true and Claude received the nine-file context. The runner then failed parsing an empty earlier handler stdout from the pre-install phase. It stopped before interpreter removal or repair.

This is an incomplete C07 attempt. Original commands, debug output, events and executed source copies are retained. Before/after normal-profile inventory was not completed, so this attempt makes no unchanged-profile claim. A fresh corrected trial follows separately.
