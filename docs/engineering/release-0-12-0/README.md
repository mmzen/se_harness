# Release 0.12.0

The functional assessment of 2026-08-30 executed: the per-change ceremony cut (`WO-ECP-021`, `WO-ECP-023`), the CLI shape normalized (`WO-ECP-022`), authoring advisories apart from the count (`WO-AUT-004`), the compatibility floor of the owner's 2026-08-30 decision (`WO-REB-031`, `WO-HUP-012`, `WO-LRE-002`), the delegation class (`WO-ECP-018`), `next` and `accept-candidate` removed (`WO-ECP-019`, `WO-ECP-020`), upgrades that retire departed files (`WO-DST-022`), the missing pages with the generated diagnostic-code index (`WO-TCM-003`), and the 0.11.0 root adoption that moved the candidate to 0.12.0 (`WO-HUP-011`).

- `REL-SEH-023`: the release contract; fourteen gates, fourteen recorded untraced exemptions, and the trace repair for the five members whose merge commits carried no parsed trailer.
- `WO-RLS-018`: cut, qualify and build the candidate; the five empty trace commits; the recipe-bound build of record.
- `VREC-SEH-021`: the aggregate verification record over all fourteen gates.
- `RLS-SEH-021`: the release record binding the build of record.
