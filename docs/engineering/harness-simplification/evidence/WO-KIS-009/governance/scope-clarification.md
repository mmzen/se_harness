# Current operator note included in the accepted scope

The accepted proposal explicitly includes updating current operator notes with
the single execution route. Inspection identified the local execution section
of `docs/notes/developing-se-harness.md` as one of those notes. Its exact path
is included before that edit. This refines the implementation file list for
KIS-EXE-006; it adds no product behavior, release/adoption action or new decision.

The full suite also identified the command walkthrough in
`docs/notes/harnessctl-check.md`, the generated diagnostic index
`docs/notes/diagnostic-codes.md`, and the existing template guidance checks in
`tests/test_managed_template_texts.py`. Include those exact paths before fixing
the stale route descriptions and expectations. These are documentation and test
consequences of the approved behavior, with no additional capability or authority.
