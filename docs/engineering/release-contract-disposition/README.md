# Release-contract disposition

This packet governs explicit disposal of six historical per-feature release proposals that remained `draft` after their work was released through later aggregate contracts.

Chain: `INT-RCD-001` -> `CAP-RCD-001` -> `REQ-RCD-001` -> `SPEC-RCD-001`, `VER-RCD-001` -> `WO-RCD-001`.

On 2026-08-16, after reviewing the exact released lineage, the accountable repository owner instructed `ok go for rejection`. That decision approves the bounded packet and authorizes only the six `draft -> rejected` transitions, their factual disposition notes, the six domain-index corrections, and retained evidence. It does not authorize changes to historical release records, aggregate release contracts, tags, publication, deployment, commits, pushes, pull requests, verification transitions, or releases.

No architecture artifact or ADR applies. This maintenance records lifecycle disposition and existing release lineage without changing software structure, behavior, dependencies, interfaces, trust boundaries, or release semantics.

The bounded implementation is complete. Exactly six unused proposals are `rejected`, `definition_pending` is empty, and the authoritative `0.2.0` and `0.2.1` aggregate release records remain unchanged. Results are retained in `evidence/WO-RCD-001-verification.md`; commit-bound verification and every external lifecycle action remain separate.
