# WO-REB-016 Pages generation-view evidence

## Retained publication outcome

Publication run `32599307612` successfully resolved released authority, rebuilt and qualified exact C6, verified and materialized the immutable GitHub Release, published the exact wheel and sdist to PyPI through Trusted Publishing, and reconciled the `release/0.6` maintenance line. Pages build job `97095372576` then failed in `Generate and package the exact Explorer payload`.

The immediately preceding predecessor-publication adapter passed. Its temporary exact two-omission checkout was then deleted as designed. The next command ran the released root dashboard generator against the full governance checkout, so immutable 0.5 semantics reported E009 for rejected `RLS-SEH-009` and E010 for duplicate version 0.6.0 across `RLS-SEH-009` and released `RLS-SEH-012`. Pages deployment was skipped; every already published release identity remained exact.

## Bounded correction

The adapter accepts one optional external absent `--view-output`. It still derives the exact rejected pair from released `RLS-SEH-012`, its bootstrap contract, and canonical preparation evidence; validates complete current governance; proves the immutable evaluator wheel/runtime/payload; and requires released 0.5 doctor and validate to pass in the temporary sparse checkout. While the source bracket remains unchanged, it atomically moves that proven clean detached checkout to the requested external directory. On any post-move failure it removes the retained view and fails closed.

Both the release workflow and standalone Pages recovery now pass a fixed runner-temporary destination and generate with the retained checkout's own root generator. They accept no omission list or expected error. The full governance checkout remains the immutable provenance and evidence source, while the generated snapshot retains the same governance commit because both checkouts are detached at that commit.

## Pre-candidate review

The initial changed path set is exactly:

- `.github/workflows/publish-dashboard-pages.yml`
- `.github/workflows/publish-pypi.yml`
- `repository_tools/predecessor_publication.py`
- `scripts/validate_predecessor_publication_view.py`
- `tests/test_dashboard_publication.py`
- `tests/test_predecessor_publication.py`
- `tests/test_release_orchestration.py`
- `docs/engineering/released-evaluator-boundary/work-orders/WO-REB-016.md`
- `docs/engineering/released-evaluator-boundary/evidence/WO-REB-016-pages-generation-view.md`

Focused adapter and publication-policy regression passed 50 tests. The stopped untracked `RLS-SEH-008` remains excluded and unchanged at SHA-256 `eea7a9953767e6b817754a517db72a2484561462fce1c9e440c5e5d1501a75fc`.

The first exact local packaging replay proved the adapter and generator but the packager rejected a retained checkout whose leaf directory was not the canonical name `governance`. The two workflow destinations were bounded to nested isolated directories ending in `/governance`, and policy tests now require both the isolation parent and canonical leaf. This correction changes no selected commit, sparse bytes, artifact, or payload content.

## Preserved identities

- C6 candidate: `3b339e9fc70cc634e6dc6bda07ea6a9b1a465798`
- annotated tag object: `03cae3d30ea1e3933a92c9e87683b0144f8ccc77`, peeled to C6
- released record: `RLS-SEH-012`
- wheel SHA-256: `2a952eb6ff4ea137d0904c3c9a6f19c88482bfbaa18a9766e5ad4d4a6fef62f7`
- normalized sdist SHA-256: `9493aa40ffbaf021edd205d6c302d67d11975bc057f73ba09b91043a9a51bbe4`
- released root evaluator: immutable 0.5 schema-2

## Pending exact qualification

Exact candidate commit identity, retained-view replay, dashboard package result, full suite, graph/distribution/portable checks, commit-bound assurance, trusted-main integration, hosted Pages recovery, public endpoint verification, and final reconciliation will be added after they occur. No result is inferred from this pre-candidate review.
