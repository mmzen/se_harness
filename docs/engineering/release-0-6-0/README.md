# Release 0.6.0 Engineering Domain

> Repository-owned index. Formal artifact authority comes from TOML metadata, typed relations, and lifecycle state—not this directory or index.

This domain defines the proposed integration and qualification of SE Harness 0.6.0 from the immutable `v0.5.0` baseline. It restores the ordinary aggregate release lifecycle after the exceptional 0.5.0 publication without rewriting that history.

## Approved release packet

- `REL-SEH-007`: exact 0.6.0 release unit, compatibility boundary, aggregate-assurance requirements, promotion gates, and rollback policy.
- `WO-RLS-008`: bounded versioning, integration, reproducibility, package acceptance, retained evidence, and aggregate-VREC preparation work.
- Proposed aggregate verification record after an approved and committed candidate: `VREC-SEH-008`.
- Proposed release record after verified aggregate assurance and separately authorized preparation: `RLS-SEH-008`.

The release contract is `approved` and `WO-RLS-008` is `in_progress`. The accountable approval authorizes bounded 0.6.0 versioning, integration, qualification, reproducible distribution builds, and retained evidence under the exact eight-work-order scope. It does not authorize the operational candidate commit, any VREC or RLS preparation or transition, tag creation or movement, publication, deployment, maintenance-line mutation, credential use, external policy change, or root-evaluator upgrade.
