# Release 0.13.0

The designed self-contained Explorer as the canonical template with no runtime CDN and the computed governance indicators (`WO-DST-023`), the first work order to travel the delegated route end to end under the enforced green gate (`WO-ECP-024`), and the 0.12.0 root adoption that moved the candidate to 0.13.0 (`WO-HUP-013`).

- `REL-SEH-024`: the release contract; four gates, a clean census with no exemptions, the build of record taken from the hosted pinned producer.
- `WO-RLS-019`: cut, qualify and build the candidate.
- `VREC-SEH-022`: the aggregate verification record over all four gates.
- `RLS-SEH-022`: the release record binding the build of record.
