# Release 0.14.0

The 0.13.0 root adopted (`WO-HUP-014`), released so that its integration commit carries the designed self-contained Explorer's generator and the release-bound public demonstration finally renders that page. The package changes only by its version markers.

- `REL-SEH-025`: the release contract; two gates, a clean census with no exemptions, the build of record taken from the hosted pinned producer.
- `WO-RLS-020`: cut, qualify and build the candidate.
- `VREC-SEH-023`: the aggregate verification record over both gates.
- `RLS-SEH-023`: the release record binding the build of record.
