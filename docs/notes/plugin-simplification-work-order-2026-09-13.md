# Plugin simplification work order

WO-PLG-021 records the owner's accepted KISS proposal. Its eleven definitions are approved through the repository's released evaluator.
This is the definition delivery for delegated implementation.

The work has six parts:

1. Delete the old harness skill directories, including edits and extra files.
2. Finish interrupted replacement by rerunning the same command.
3. Keep one portable plugin choice through doctor and upgrades.
4. Create, reuse or repair the private checker environment.
5. Read instructions normally and use explicit checks; remove automatic blocking hooks.
6. Simplify local assembly and run a smaller useful test set.

SPEC-PLG-021 includes the exact prior-contract applicability table. The proposal coverage map accounts for all 50 reviewed scenarios.
It keeps seven useful protections and requires 43 removals or simplifications.

The packet was prepared from main 66a4b8a43409320d01cfe5c67cc9657600ed4681 in a separate worktree.
The prior local prototype remains separate and is not a completed implementation of the whole work order.
The released 0.17.0 evaluator checked the formal graph and root integrity and applied all eleven approvals.
No old verification, release or decided decision record was changed.

The delegated route requires this approved packet at the pull-request base and a successful required check for the candidate commit.
That rule is DR-015 in docs/engineering/DECISION_RIGHTS.md. No implementation start is recorded by this definition delivery.
