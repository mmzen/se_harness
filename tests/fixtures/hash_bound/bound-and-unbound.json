{
  "classes": [
    {
      "bindings": ["shared_sha256"],
      "id": "only",
      "mode": "raw",
      "patterns": ["docs/only.json"],
      "region": "repository",
      "required_attribute": "text eol=lf"
    }
  ],
  "schema": "se-harness-hash-bound-classes-v1",
  "unbound_digest_fields": [
    {
      "field": "shared_sha256",
      "reason": "declared out of scope while also bound"
    }
  ]
}
