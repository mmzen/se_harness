# Fixed WO-PLG-012 retained-helper oracle

Fixed before reading the candidate product SKILL. Authority is the approved
REQ-PLG-020/021, SPEC/VER-PLG-012 plus the existing managed orient.py,
check_brief.py and their skill-contract.json files. The tested evaluator is the
actual external released 0.16.0 on Windows and Linux; source governance at 0.17
is not the fixture evaluator. Helpers and contracts must match existing managed
source bytes. C04/C05/C07 activation/rendering decisions belong to a separate
independent observer; this runner does not select or invent them.

The raw fixture is a minimal approved INT/CAP/REQ/SPEC/VER/WO chain with a local
greeting source. Initial approved states are explicit synthetic setup, never an
exercise of source authority. Each OS performs real init, copies only authored
raw content, creates native fixture Git, and validates. No Windows lock, venv,
or Git data is copied to Linux. Sentinel files, including an ignored file, are
set up before measurement. All files and all Git metadata are independently
hashed before/after every helper call.

| Case | Fixed expectation |
|---|---|
| C01 | Actual packaged orient with selected WO and exact absolute launcher: exit0, completed or documented degraded; schema se-harness-orientation-result-v1; receipt schema se-harness-execution-receipt-v1; changed_paths=[]; worker_results=[]; identity/doctor/validate/inspect and selected check occur; no target write. |
| C02 | Same input with --preflight-phase start adds preflight; absent option retains preflight.status=not_requested; no saved receipt in either run. |
| C03 version | Direct deliberate wrong expected version exits2, blocked, AEXORI013; only version operation. This is a helper negative probe, not the skill's preexecution trust decision. |
| C03 root/payload | Direct deliberately wrong expected root or exact payload input exits2, blocked; identity fails, no later inspection or repair. Payload corruption is confined to a new disposable fixture lock and fixed before measurement; source locks remain untouched. |
| C06 | Fixed bounded source and spans: wrong source SHA exits2 TCM006; reversed spans exits2 TCM007; changed protected output bytes exits2 TCM010. Inputs remain unchanged. Valid helper control exits0 with original digest and span count; it is a helper calibration, not a newly rendered skill brief. |
| C08 | Calibrate real attempted target write, socket creation, synthetic credential read, unexpected process/worker spawn and forbidden evaluator lifecycle operation. Each is denied and logged before effects. Then the helper cases must have no denied-effect events, no unexpected subprocesses, no target/Git/ignored file changes. Approved read-only evaluator/Git calls have separate nonempty allow logs. |

The test-only CPython wrapper installs an audit hook, permits only fixed absolute
read-only evaluator/engine/Git command vectors, and propagates itself to actual
packaged evaluator engine scripts. It executes unchanged source with runpy;
evaluator outputs are never mocked. Required Python child argv wrapping and Git
executable resolution are transparent in original/effective process logs. HOME,
Git configuration, and environment are isolated; no real credentials are used.

This is a calibrated test boundary around CPython-visible operations, not a
production sandbox, native host qualification, universal shell/API enforcement,
or defense against malicious native extensions. Fixed read-only Git subprocesses
run outside Python auditing; their exact argv, isolated config, and full before/
after target hashes bound that surface. No delegation is performed inside any
single helper invocation. Empty denied logs alone are not acceptance evidence.

Keep original fixture/setup failures and execution errors. Snapshot every
executed runner/helper source before each run; retain argv/stdin/stdio/status,
boundary logs and source identities. Do not silently replace failed evidence.
