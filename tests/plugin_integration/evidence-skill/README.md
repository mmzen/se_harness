# Evidence skill acceptance

The product is instructions over released commands. Independent instruction
observations fix the decisions and expected effects before reading the skill,
then record actual calls, file hashes and lifecycle readbacks in disposable
repositories. A recorder must not implement the authority decisions being tested.

VER-PLG-011 selects EVD01–10. External merge and publication tools are intercepted;
fixtures have no production credentials or remote write path. Successful
interception is a fixture observation, not production enforcement qualification.
Local fixture commits are necessary to compare candidate C with later governance
G. Original failures and corrected repeats stay separate.

The portable replay checks the recorded command sequence with a separately
verified Linux evaluator and freshly initialized managed repository. It does
not count as new model reasoning. Native plugin activation is separate work.

Acceptance execution is in progress; retain results under the selected WO's
evidence directory before reporting completion.
