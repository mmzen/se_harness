# Independent WO-PLG-011 evidence skill acceptance

The corrected skill satisfied the bounded Windows instruction observations below. One original capture-footprint failure remains retained: the initial record/evaluator-only expectation omitted 14 generated dashboard files. The corrected reference now names that footprint, and a fresh capture passed a separately fixed expectation. This report supplies acceptance evidence, not an assurance or release decision.

Oracle SHA256 `4b791ff512aeb370b6a469789e5a4865a3744759c4b377daeb0c286c72fad401` and raw-scenarios SHA256 `d13f94233ad1b751868f92d8b3e6856ebd8ffb005490d22aff977a18f040e7bd` were fixed before candidate inspection from approved SPEC/VER-PLG-011, SPEC-PLG-010 applicability and installed 0.16.0 contracts. Detailed fixture artifacts were then authored from the installed artifact shapes and frozen before their operations. No source worktree, managed lock or evaluator was edited.

The actual external Windows Python was 3.14.6, released evaluator 0.16.0, with trusted archive `a969d6ab9e80acc2c9f9e7b6679a02e7ffab371f1f11cb0c0f4243f208ed9eae` and payload `51712fcfe5253db8d542deb870a0017b25c2976f5f4e76a81fd08923bba45e3c`. Identity and managed integrity passed. Invocation used `-I -B -m se_harness` outside checkouts, cleared PYTHONPATH and environment Scripts first. Complete actual handler output was read; subsequent identical governance bodies were byte-compared with that fully read body after fresh per-repository handler verification. This was direct handler testing, not native host-hook qualification.

| Case | Observation and exact limits |
|---|---|
| EVD01 | Actual greeting assertion exit 0, injected wrong expectation exit 1, and explicitly unrun probe retained with commands, output, paths/digests and tested commit `97f95e197f4bb7194bcf76a58306e6cff38c37b9`. Runtime identity is retained in raw trace. Failed/unrun probes were not candidate acceptance requirements or invented successes. |
| EVD02 | Actual capture produced ready VREC; actual prepare-release produced ready RLS. File/state readback and checkpoint-free checks agreed; related WO/VREC bytes stayed unchanged. Initial VREC footprint expectation failed; corrected fresh capture passed. RLS wrote exactly record plus evaluator sidecar. Its preverified VREC is an explicitly synthetic preexisting fixture decision, not an exercised assurance right. |
| EVD03 | Real capture exit completed, receipt deliberately hidden, then inspect-first readback showed existing ready VREC; no replay. Separate completed capture with unavailable inspection had no later writers. Additional fresh injected partial state copied only actual VREC bytes, omitted sidecar, then inspection failed; all partial-state hashes stayed unchanged with no recovery writer. These are post-exit receipt/inspection faults and an injected partial state, not in-flight crash or atomicity tests. |
| EVD04 | Separate merge/publication cases for missing exact authority, failed fixture gate and unproved control coverage: no dispatch/effect, unchanged local and simulated state hashes, precise model blocker. Independent positive and negative control calibration makes this more than an empty-log assertion. |
| EVD05 | All four actual writers exercised with supplied authority; all four absent-authority instruction cases avoided dispatch. Actual read-only preflight changed no files. Handoff first refused missing packet; actual evidence created it; repeated handoff passed and wrote JSON. The original refusal is retained. Capture footprint follows the EVD02 correction. |
| EVD06 | Dirty tracked candidate observed and skill stopped before capture. A distinct, explicitly labeled CLI supplement called actual released capture and received WEX302, exit 1, zero changed files and no VREC. It is not counted as an instruction-selected capture. Unborn-HEAD variant unrun. |
| EVD07 | Ready VREC binds clean candidate C `772f90c03a04d0a7914088ac6b869bb829b4bcad`. Later local governance G `cad90fac47a37d92f9eb94133f069dff6d33adbc` retains it without rebinding; C != G, WO unchanged, no verified fields. |
| EVD08 | Separate full requested commit, evidence digest, action and destination drift, plus assurance-only-for-merge, all stopped before dispatch with unchanged hashes. These test supplied input identity drift; they do not create a fresh Git head for each counterfactual. |
| EVD09 | Exact synthetic repository-owner decision supplied separately from controls. Actual released integration pre-action check passed QG-G4-VERIFIED-COVERAGE. Same C/digest/action/destination and calibrated fixture control allowed exactly one original project-tool dispatch and one simulated ref update, zero duplicate approval prompts. |
| EVD10 | Otherwise authorized capture with absent current context: checkpoint-free check remained read-only, no writer ran, model directed setup verification plus complete context delivery. It did not treat the lifecycle projection as readiness. Absent variant observed; stale/incomplete context variants not separately rerun for this skill. |

The project tool's complete controlled action surface is `project_action.py`. The separate fixed allowlist/gate control rejected wrong action, commit, digest, destination (exit 17) and failed gate (exit 18); only invocation logs changed. Two positive calibration calls changed simulated refs/registry. The behavioral positive had its own initial state and one effect. No real GitHub, registry, network, credentials, merge, publication, verification or release transition occurred. These controls qualify only this fixture surface; no universal shell/API enforcement is claimed. External publication gates are synthetic provider inputs, not live CI results.

The raw authoring fixture initially used an invalid verification-method enum; real validation refused it. The original input/failure is preserved, and a separate exact correction to `test` passed. That fixture defect is distinct from the original candidate's incomplete capture write account. Some Git inspections updated only `.git/index` stat-cache bytes; the recorder retained those effects. They were not silently filtered from all-file hashes.

SKILL SHA256 stayed `647c20a982c798c0617145cbcfe2f3356d23b4f827641b6affff9592bf02ad3b`. Original records reference was `46802aff7f8ebc7200b0908beba3957c99f4389fc384c8d8ae84da531575eae3`; corrected is `0c266bef118b904030e1982318763021bdfd699e34b353d4f38e5d9f2f43a20b`. Corrected external reference `ec0fd8f071f2bb0d5ae294594df7f118a817d0c62c8616c26e22285281acbb69` was used for external cases. Its read-only inspection clarification came from another reviewer; the original ambiguity was not independently observed here.

`audit.json` asserts retained state/hash/invocation invariants and records 82 exact traces. `trace-groups.json` supplies order and clone boundaries; `final-hashes.json` retains fixture and candidate bytes. Approval-prompt count for the fixture observations was zero, including no duplicate prompts for unchanged preparation or recovery. Counterfactual variants were grouped in this independent model session, not fresh native host conversations. Linux replay is delegated separately and must be labeled command regression, not additional independent model reasoning. Production external controls, live CI, native host enforcement and in-flight crash behavior remain unqualified by this pass.
